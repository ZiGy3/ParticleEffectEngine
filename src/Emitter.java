import java.util.Random;

// Eventually emitter will spawn particles. Emitter will have direction and particles will have direction tied to
// emitter's direction. Particles will have initial position same as emitter or in a half circle or line.
public class Emitter {
	private static int pNum = 10;
	private static Particle[] particles = new Particle[pNum];
}
